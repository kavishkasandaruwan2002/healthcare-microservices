server:
  port: 8087

spring:
  application:
    name: telemedicine-service

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka/}

agora:
  appId: ${AGORA_APP_ID}
