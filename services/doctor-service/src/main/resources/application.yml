server:
  port: 8080

spring:
  application:
    name: doctor-service
  datasource:
    url: jdbc:mysql://mysql:3306/doctor_service
    username: root
    password: password
  data:
    mongodb:
      uri: mongodb://mongo:27017/doctor_service

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
