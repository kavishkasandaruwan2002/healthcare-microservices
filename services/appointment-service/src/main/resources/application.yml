server:
  port: 8080

spring:
  application:
    name: appointment-service
  datasource:
    url: jdbc:mysql://mysql:3306/appointment_service
    username: root
    password: password
  data:
    mongodb:
      uri: mongodb://mongo:27017/appointment_service

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
