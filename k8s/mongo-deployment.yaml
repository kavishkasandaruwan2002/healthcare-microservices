apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo
  namespace: healthcare
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo
  template:
    metadata:
      labels:
        app: mongo
    spec:
      containers:
      - name: mongo
        image: healthcare/
        ports:
        - containerPort: 27017
